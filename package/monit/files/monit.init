#!/bin/sh
#PKG monit
#INIT 10

. /etc/rc.conf

case $1 in
autostop) ;;
autostart|start)
	monit $monit_flags
	;;
stop)
	killall monit	
	;;
restart)
	sh $0 stop
	sh $0 start
	;;
*)
	echo "usage: $0 (start|stop|restart)"
	exit 1
esac
exit $?
