#!/bin/sh
#PKG ulogd
#INIT 49

. /etc/rc.conf

case $1 in
autostop) ;;
autostart)
	test x"${ulogd:-NO}" = x"NO" && exit 0
	exec sh $0 start
	;;
start)
	ulogd -d
	;;
restart)
	sh $0 stop
	sh $0 start
	;;
*)
	echo "usage: $0 {start | stop | restart}"
	exit 1
esac
exit $?
