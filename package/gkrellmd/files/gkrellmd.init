#!/bin/sh
#FWINIT 80
. /etc/rc.conf

case $1 in
autostop) ;;
autostart)
	test x"${gkrellmd:-NO}" = x"NO" && exit 0
	exec sh $0 start
	;;
start)
	gkrellmd -d
	;;
stop)
	killall gkrellmd
	;;
restart)
	sh $0 stop
	sh $0 start
	;;
*)
	echo "Usage: $0 {start | stop | restart}"
	exit 1
	;;
esac
exit $?
