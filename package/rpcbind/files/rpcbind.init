#!/bin/sh
#PKG rpcbind
#INIT 60

. /etc/rc.conf

case $1 in
autostop) ;;
autostart)
	test x"${rpcbind:-NO}" = x"NO" && exit 0
	exec sh $0 start
	;;
start)
	rpcbind
	;;
stop)
	pkill rpcbind
	;;
restart)
	sh $0 stop
	sh $0 start
	;;
*)
	echo "Usage: $0 {start | stop | restart}"
	exit 1
	;;
esac
exit $?
