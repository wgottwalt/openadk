#!/bin/sh
#PKG uvd
#INIT 10

. /etc/rc.conf

case $1 in
autostop) ;;
autostart)
	test x"${uvd:-NO}" = x"NO" && exit 0
	exec sh $0 start
	;;
start)
	uvd
	;;
stop)
	pkill uvd	
	;;
restart)
	sh $0 stop
	sh $0 start
	;;
*)
	echo "usage: $0 (start|stop|restart)"
	exit 1
esac
exit $?
