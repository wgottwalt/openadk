#!/bin/sh
#FWINIT 50
. /etc/rc.conf

case $1 in
autostop) ;;
autostart)
	test x"${pppoe_server:-NO}" = x"NO" && exit 0
	exec sh $0 start
	;;
start)
	pppoe-server
	;;
stop)
	killall pppoe-server
	;;
restart)
	sh $0 stop
	sh $0 start
	;;
*)
	echo "Usage: $0 {start | stop | restart}"
	exit 1
	;;
esac
exit $?
