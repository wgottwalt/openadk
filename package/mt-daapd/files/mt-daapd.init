#!/bin/sh
#FWINIT 75
. /etc/rc.conf

case $1 in
autostop) ;;
autostart)
	[[ $mt_daapd = NO ]] && exit 0
	exec sh $0 start
	;;
start)
	mt-daapd
	;;
stop)
	killall mt-daapd
	;;
restart)
	sh $0 stop
	sh $0 start
	;;
*)
	echo "Usage: $0 {start | stop | restart}"
	exit 1
	;;
esac
exit $?
